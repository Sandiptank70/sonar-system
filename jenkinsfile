pipeline {
    agent {
        label "node"
    }
    stages {
        stage("check node version") {
            steps {
                sh '''
                    node -v
                    npm -v
                '''
            }
        }
    }
}
