pipeline {
    agent {
        docker {
            image 'node:latest'
            args '-u root'
        }
    }
    stages {
        stage("check node version") {
            steps {
                sh '''
                    node -v
                    npm -v
                '''
            }
        }
    }
}
